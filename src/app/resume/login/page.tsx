import ResumeLogin from "@/components/ResumeLogin";

export const metadata = {
  title: "Resume Editor Login | Prakash Sharma",
  robots: { index: false, follow: false },
};

export default function ResumeLoginPage() {
  return <ResumeLogin />;
}
